function addition_Function() {  //Function to compute the value of a addition operation
    var addition = 2 + 2; //variable addition is defined as two operands, 2 and 2 and the operator +
    document.getElementById("Math").innerHTML = "2 + 2 = " + addition; //document.write will display value of 2 plus  2 to the HTML output
}